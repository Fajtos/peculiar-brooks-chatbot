<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Peculiar Assistant | Peculiar Brooks</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 700px;
      margin: 2rem auto;
      padding: 1rem;
      background-color: #f7f7f7;
    }

    h1 {
      text-align: center;
      color: #1a1a1a;
      margin-bottom: 0.3rem;
    }

    h4 {
      text-align: center;
      color: #444;
      font-weight: normal;
      margin-top: 0;
      margin-bottom: 1.5rem;
    }

    #chatHistory {
      background-color: #fff;
      border: 1px solid #ccc;
      border-radius: 10px;
      padding: 1rem;
      height: 400px;
      overflow-y: auto;
      margin-bottom: 1rem;
    }

    .user-msg {
      background-color: #d0ebff;
      padding: 0.8rem;
      border-radius: 8px;
      margin: 0.5rem 0;
      text-align: right;
      white-space: pre-wrap;
    }

    .bot-msg {
      background-color: #e6f4ea;
      padding: 0.8rem;
      border-radius: 8px;
      margin: 0.5rem 0;
      text-align: left;
      white-space: pre-wrap;
    }

    textarea {
      width: 100%;
      padding: 0.8rem;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 1rem;
      resize: none;
      margin-bottom: 1rem;
    }

    button {
      padding: 0.8rem 1.2rem;
      font-size: 1rem;
      border: none;
      background-color: #0077cc;
      color: white;
      border-radius: 8px;
      cursor: pointer;
    }

    button:hover {
      background-color: #005fa3;
    }

    .intro {
      font-size: 0.95rem;
      background-color: #fff9e6;
      padding: 1rem;
      border-left: 4px solid #ffcc00;
      border-radius: 8px;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>

  <h1>Ask Peculiar 🌟</h1>
  <h4>Peculiar Brooks | Cloud Security Support Assistant</h4>

  <div class="intro">
    <strong>Welcome to Peculiar Assistant!</strong><br>
    This assistant was built by <em>Peculiar Brooks</em> to help internal team members, IT admins, and cloud engineers with quick answers about AWS, IAM policies, S3 usage, cloud security, and more. Ask anything from “How do I create a presigned URL?” to “How do I detect unauthorized access with CloudTrail?”
  </div>

  <div id="chatHistory"></div>

  <textarea id="question" rows="4" placeholder="Type your question here..."></textarea>
  <button onclick="askPeculiar()">Ask</button>

  <script>
    const chatHistory = document.getElementById('chatHistory');

    async function askPeculiar() {
      const questionInput = document.getElementById('question');
      const question = questionInput.value.trim();

      if (!question) {
        alert("Please enter a question.");
        return;
      }

      appendMessage('user', question);
      questionInput.value = '';

      const loadingMsg = appendMessage('bot', 'Thinking...');

      try {
        const response = await fetch('https://11uolqxvda.execute-api.us-east-2.amazonaws.com/ask', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ question })
        });

        const data = await response.json();
        loadingMsg.textContent = data.answer || "No answer found.";
      } catch (err) {
        loadingMsg.textContent = "Error: " + err.message;
      }
    }

    function appendMessage(sender, text) {
      const msgDiv = document.createElement('div');
      msgDiv.className = sender === 'user' ? 'user-msg' : 'bot-msg';
      msgDiv.textContent = text;
      chatHistory.appendChild(msgDiv);
      chatHistory.scrollTop = chatHistory.scrollHeight;
      return msgDiv;
    }
  </script>

</body>
</html>
